---
"spatial-plugin-grid": patch
---

Preserve the public bundle's React client boundary and verify it in packed React 18/19 consumers. Document embedded dashboard sizing, client-owned workspace definitions, and the generic API release prerequisite.
