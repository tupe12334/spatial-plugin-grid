---
"spatial-plugin-grid": minor
---

Add a generic `createListPlugin` and `ListBlock` export so hosts and agents can mount any data collection as a validated compact/expanded list plugin without bespoke plugin code per data type.
