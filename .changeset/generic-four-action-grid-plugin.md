---
"spatial-plugin-grid": minor
---

Add a generic `createFourActionGridPlugin` and `FourActionGrid` export: a titleless single-cell plugin with exactly four equal, border-separated action quadrants. Hosts supply typed action IDs, labels, optional icons and handlers.
